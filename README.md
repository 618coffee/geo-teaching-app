# Geo Teaching App

面向高中地理"工业区位选择"场景的教学应用 MVP。

当前版本将学生端、教师端、运营端放在同一个 React 单页应用中，用示例数据先跑通页面结构、路由切换、教学流程和分析展示，方便继续验证交互方案、课堂流程和后续平台化扩展。

## 项目目标

- 验证高中地理"工业区位选择"教学场景的完整前端链路
- 在没有后端服务的前提下，用示例数据模拟任务、选址、分析报告和管理页面
- 为后续接入真实 API、鉴权、题库配置和 AI 能力保留清晰的页面骨架

## 项目结构

```text
.
├── frontend/           # React 前端应用（详见 frontend/README.md）
├── backend/            # Spring Boot 认证与 API 后端（详见 backend/README.md）
├── docs/               # 部署方案、接口设计等文档
└── package.json        # 根级便捷脚本
```

## 快速开始

### 运行环境

- Node.js 18+、npm 9+（前端）
- Java 21+（后端）

### 前端

```bash
cd frontend
npm install
npm run dev
```

默认启动在 `http://localhost:5173`。

### 后端

```bash
cd backend
sh ./mvnw spring-boot:run
```

默认启动在 `http://localhost:4000`。

### 根级便捷脚本

也可以从项目根目录运行：

```bash
npm run dev          # 启动前端
npm run dev:server   # 启动后端
npm run build        # 构建前端
npm run build:server # 构建后端
```

## 更多文档

- 前端详细说明：`frontend/README.md`
- 后端详细说明：`backend/README.md`
- 阿里云认证与部署方案：`docs/aliyun-auth-backend-plan.md`

## 国内后端选型建议

如果这个应用的主要用户面向中国大陆，默认推荐优先考虑阿里云，原因是：

- 短信服务、邮件推送、RDS、Redis、对象存储、CDN、WAF 和备案配套都比较完整
- 国内教育、内容平台和中后台系统的实践案例更多，资料和第三方生态也更成熟
- 如果后续要做高并发验证码、静态资源分发、日志审计和多地域容灾，落地路径相对清晰

建议的第一版后端组合：

- 计算层：阿里云 SAE 或 ECS
- API 层：Spring Boot / NestJS / Go 任一常规 Web 服务
- 数据库：阿里云 RDS MySQL
- 缓存：阿里云 Redis，用于验证码、登录限流、短时会话缓存
- 短信：阿里云短信服务
- 邮件：阿里云邮件推送
- 文件：阿里云 OSS

如果你的产品后续会深度接入微信生态，可以把腾讯云作为第二候选；如果更偏政教项目、对本地化交付和国产化要求更高，可以再评估华为云。

从当前这个项目的需求看，首选结论可以先定为：

- 默认选阿里云
- 如果强绑定微信生态，再看腾讯云
- 如果是高校/政教采购驱动，再看华为云

## 后续可以继续演进的方向

- 接入真实任务、班级、案例库和提交记录 API
- 将当前前端演示认证迁移为服务端账号体系、验证码服务和权限控制
- 将当前分析逻辑替换为可配置规则引擎或 AI 服务
- 补充测试、环境变量和部署文档
